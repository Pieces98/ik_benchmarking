# This branch targets the MoveIt Pro container only. The pose_ik plugin is provided by the
# MoveIt Pro overlay (no source pull required). External IK-solver sources are intentionally
# omitted — they belong on the mw2-moveit2-ik-benchmarking branch.
repositories: {}
